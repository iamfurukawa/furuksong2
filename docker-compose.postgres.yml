version: "3"
services:
  # PostgreSQL Database Genérico (não exposto publicamente)
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: furuksong2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./srv/postgres:/docker-entrypoint-initdb.d
    networks:
      - nginxnet
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d furuksong2"]
      interval: 10s
      timeout: 5s
      retries: 5

  # PgAdmin exposto via NGINX
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@furuksong2.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - nginxnet
    depends_on:
      - postgres

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local

networks:
  nginxnet:
    external: true